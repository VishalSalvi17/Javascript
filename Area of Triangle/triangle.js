const basevalue = prompt('Enter the value of base:');
const heightvalue = prompt('Enter the value of height:');

//calculate area of triangle
const area = (basevalue * heightvalue)/2;

//display area
document.write(`The area of triangle is ${area}`);